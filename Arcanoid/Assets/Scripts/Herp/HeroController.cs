using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeroController : MonoBehaviour
{
    [HideInInspector] public HeroModel Model = new HeroModel(); 

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
